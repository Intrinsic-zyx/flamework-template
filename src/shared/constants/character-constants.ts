import { Players } from "@rbxts/services";
import { promiseR6 } from "@rbxts/promise-character";

export const CHARACTER_RESPAWN_DELAY = Players.RespawnTime;
export const CHARACTER_PROMISE_FUNCTION = promiseR6;
